# MySQL 数据源：库需预先存在（建库建表脚本见项目根目录 db/schema.sql）
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/smart_agri_loan?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 114514

  jpa:
    hibernate:
      # 演示环境用 update 自动建表；生产建议改为 validate 并手工执行 db/schema.sql
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.MySQLDialect
